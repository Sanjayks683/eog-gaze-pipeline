# Versions the committed results were produced and tested with (Python 3.11.9, Windows 11,
# NVIDIA RTX 4060 with CUDA 12.1). Newer versions will probably work, but XGBoost and PyTorch
# results can differ slightly between versions and between CPU and GPU.
#
# For GPU training install the CUDA build of PyTorch first, e.g.
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
numpy==2.4.6
scipy==1.17.1
pandas==3.0.3
scikit-learn==1.9.0
xgboost==3.2.0
torch==2.5.1
matplotlib==3.11.0
PyYAML==6.0.3
pytest==9.0.3
